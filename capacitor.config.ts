import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mscode.LaGranjaApp',
  appName: 'LaGranjaApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
